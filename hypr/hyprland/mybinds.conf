#Return application launcher
bind = Super, O, submap, appsubmaps
submap = appsubmaps

bind = , b, exec, librewolf
bind = , b, submap,reset

bind = , q, exec, qutebrowser
bind = , q, submap,reset

bind = , m, exec, pypr toggle ncmp
bind = , m, submap,reset

bind = , n, exec, kitty -e newsboat
bind = , n, submap,reset

bind= , escape, submap, reset
submap = reset


# state shortcuts
bind = Super, P, submap, statesubmaps
submap = statesubmaps

bind = , l, exec, hyprlock
bind = , l, submap, reset

bind=,escape,submap,reset
submap = reset


# generic stuff
bind = $mainMod, w, exec, wallscheme
